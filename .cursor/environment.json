{
  "build": {
    "dockerfile": ".cursor/Dockerfile",
    "context": ".."
  },
  "install": "mix local.hex --force && mix local.rebar --force && mix deps.get && mix compile && cp .githooks/pre-commit .githooks/pre-push .git/hooks/ && chmod +x .git/hooks/pre-commit .git/hooks/pre-push"
}
